{
  "name": "slh",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "echo 'Add later'",
    "test": "echo 'Add later'"
  }
}